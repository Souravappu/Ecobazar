<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/js/all.min.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #f8f9fa;
            min-height: 100vh;
            background-image: linear-gradient(rgba(0, 0, 0, 0.7), rgba(0, 0, 0, 0.7)),
                             url('https://images.unsplash.com/photo-1542838132-92c53300491e?q=80');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
        }
        .nav-link:hover {
            color: #00b300;
        }
        .error-message {
            color: red;
            font-size: 0.75rem;
            margin-top: 0.25rem;
        }
        .success-message {
            color: green;
            font-size: 0.75rem;
            margin-top: 0.25rem;
        }
    </style>
</head>
<body>
    <!-- Top Navigation -->
    <nav class="p-4 text-white flex items-center gap-2">
        <a href="#" class="text-white hover:text-green-500">
            <i class="fas fa-home"></i>
        </a>
        <span class="text-gray-500">/</span>
        <span>Account</span>
        <span class="text-gray-500">/</span>
        <span class="text-green-500">Forgot Password</span>
    </nav>

    <!-- Forgot Password Form -->
    <div class="flex justify-center items-center min-h-[80vh]">
        <div class="bg-white rounded-lg shadow-lg p-8 w-full max-w-md mx-4">
            <h2 class="text-2xl font-semibold text-center mb-6">Forgot Password</h2>

            <% if (error) { %>
                <p class="error-message"><%= error %></p>
            <% } %>

            <% if (success) { %>
                <p class="success-message"><%= success %></p>
            <% } %>

            <form id="forgotPasswordForm" class="space-y-6" method="post" action="/forgot-email-valid">
                <div>
                    <label for="email" class="block text-sm font-medium text-gray-700 mb-1">Email</label>
                    <input
                        type="email"
                        id="email"
                        name="email"
                        class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-green-500 focus:border-transparent"
                        placeholder="Enter your email address"
                        required
                    >
                </div>

                <button
                    type="submit"
                    class="w-full bg-green-500 text-white py-2 rounded-lg hover:bg-green-600 transition duration-300"
                >
                    Continue
                </button>
            </form>

            <div class="text-center mt-4">
                <span class="text-gray-600">Already have an account? </span>
                <a href="/login" class="text-green-500 hover:text-green-600 font-medium">Login</a>
            </div>
        </div>
    </div>
</body>
</html>
